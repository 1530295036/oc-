iOS对象一旦创建之后，不可变对象封装的值就是不可变的，且在整个对象的生命周期都保持不变。
但是对于可变对象，可以随时修改的封装的值。
对象在默认的情况下都是可变的，大多数对象都允许通过setter存储方法改变其封装的数据。
Foundation框架引入一些具有可变变体和不可变变体的类明确了一些细微的差别。可变的子类通常是其不可变变体的子类，且在类名上嵌入了Mutable字样。
这些类详情见692页
对象的不可变在理论上保证了对象的值时稳定的。
但在实践中，类的方法可能会将不可变变体的返回类型下的可变对象取出，并在之后决定对其进行改变，这可能侵犯接收者根据之前的值做出的假定和选择
