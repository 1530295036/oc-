autorelease为后续的释放 标记了 接收者。
用于声明：在发送消息的作用域之外，不想再保留该对象。这个作用域的范围是由当前的自动释放池定义的。
使用alloc创建数组，因此将拥有该数组，并负责在使用后释放所有权。
当一个方法得到该对象的数组是，这个方法假设：当不在需要该数组是，会将其销毁，但仍可以在其作用域内安全的使用该数组。
该方法设置可以将数组返回给他的调用者，因为应用程序对象为代码定义好了调用堆栈的底部。
对象只能在方法的内部引用新的数组对象。
在发送release消息后，新数组不再具有所有者，所以会被系统立即销毁。因此，该方法返回了一个无效（已释放）的对象
