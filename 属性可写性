以下两个特性负责控制一个属性是否有setter方法，它们时互斥的

Readwrite标志一个属性时可读写的，这个特性是默认的，表面在@implementation中需要实现getter和setter两个方法。
如果实现中使用了@synthesize指令，那么getter和setter方法会有编译器自动生成。

Readonly标志一个属性为只读。
如果标明readonly，那么在实现中使用了@synthesize指令，那么编译器只生成getter方法。另外如果在代码中使用点语法为属性赋值，那么编译器会报错
