
可以通过@property(attribute[,attribute2,......])使用特性来修饰一个属性
与方法相同，属性的作用范围仅限于其接口声明以内。
在属性声明中可以使用逗号分隔多个属性名，这样特性会应用到所有的属性名上。
如果使用@synthesize指令同志编译器创建访存方法，生成的代码会与给定的特性关键字匹配。
如果要自己实现访存方法，需要确保它符合这些特性（例如，指定来copy特性，就必须确保在存储方法中复制输入值）。
