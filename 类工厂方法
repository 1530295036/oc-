类工厂方法就是将分配和初始化结合在一起，返回被创建的对象，并进行自动释放处理
这些方法的形式时+(type)className(其中，className不包括任何前缀）
工厂方法 可以为初始化过程 提供对象的 分配信息。在工厂方法确定一个为集合类分配多少内存之前，必须先读取文件并对属性列表进行解析，确定有多少元素，每个元素的类型是什么。
另一个目的是使类提供单独的实例。可以避免为可能没有用的对象盲目分配内存。
