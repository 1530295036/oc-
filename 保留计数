所有权策略是在调用retain方法后通过引用计数（保留计数）实现的。每个对象都有一个保留计数。
当创建一个对象时，该对象的保留计数为1.
当向一个对象发送retain消息时，该对象的保留计数加1.
当向一个对象发送release消息时，该对象的保留计数减1.
当向一个对象发送autorelease消息时，该对象的保留计数将会在将来的某个阶段减1.
如果一个对象的保留计数被减为0，该对象就会被回收。通常不必显示地查询对象的保留计数是多少（参考retainCount）。
其结果往往容易产生误导，因为可能不知道感兴趣的对象由何种框架对象保留。
